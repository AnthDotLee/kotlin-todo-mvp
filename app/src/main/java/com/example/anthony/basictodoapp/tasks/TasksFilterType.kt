package com.example.anthony.basictodoapp.tasks

// Enum for various filtering types. All tasks, completed ones, or remaining tasks
enum class TasksFilterType {
    ALL_TASKS, ACTIVE_TASKS, COMPLETED_TASKS
}